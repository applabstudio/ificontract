langcode: it
status: true
dependencies:
  config:
    - user.role.webit
  module:
    - user
id: user_add_role_action.webit
label: 'Add the Webit role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: webit
