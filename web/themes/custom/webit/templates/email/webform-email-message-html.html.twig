{#
/**
 * @file
 * Default theme implementation for webform email wrapper template as HTML.
 *
 * Available variables:
 * - message: The email message which contains to, from, subject, message, etc...
 * - webform_submission: The webform submission.
 * - handler: The webform handler.
 *
 * @ingroup themeable
 */
#}

<html>
<head></head>
<body>
<img src="{{ drupal_url(active_theme_path() ~ '/images/logo.png', {absolute: true}) }}" width="240" alt="{{ drupal_token('site:name') }}"><br><br><br>
{{ message.body }}
<div class="container">
  <address class="address">
    <h4 class="address__title">Nome azienda</h4>
    <p>
      Indirizzo<br/>
      Località - <a href="mailto:info@prova.com">info@prova.com</a><br/>
    </p>
  </address>
</div>
</body>
</html>